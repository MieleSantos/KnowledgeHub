services:
  api:
    build:
      context: .
      dockerfile: book_catalog_api/Dockerfile
    container_name: book-catalog-api
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
    volumes:
      - ./book_catalog_api/books.db:/app/books.db
      - ./book_catalog_api/app/services/semantic_search/data:/app/data
    restart: unless-stopped

  streamlit-hub:
    build:
      context: .
      dockerfile: streamlit_hub/Dockerfile
    container_name: streamlit-hub
    ports:
      - "8501:8501"
    environment:
      - API_BASE_URL=http://api:8000
    depends_on:
      - api
    restart: unless-stopped
